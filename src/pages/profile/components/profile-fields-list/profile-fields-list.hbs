<ul class="profile-fields-list">
    {{#each profileFields}}
        {{> profile-field
            label=this.label
            type=this.type
            name=this.name
            autocomplete=this.autocomplete
            value=this.value
            readonly=this.readonly
        }}
    {{/each}}
</ul>