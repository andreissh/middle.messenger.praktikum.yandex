<li class="profile-field-item">
  <label class="profile-field-label" for="{{name}}">{{label}}</label>
  <input
    class="profile-field-input"
    type="{{type}}"
    name="{{name}}"
    value="{{value}}"
    autocomplete="{{autocomplete}}"
    {{#if readonly}}readonly{{/if}}
  />
</li>